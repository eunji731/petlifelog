package com.petlifelog.backend.common.domain.code;

/**
 * [공통코드 타입]
 * tb_code의 type 컬럼에 들어갈 수 있는 값들을 열거합니다.
 * 새로운 코드 그룹이 생기면 여기에 추가합니다.
 */
public enum CodeType {
    ROLE // 회원 권한 (ROLE_USER, ROLE_ADMIN 등)
}
